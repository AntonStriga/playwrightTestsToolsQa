{
    "extends": [
        ".eslintrc",
        // Только изменённые части изменённых файлов
        "plugin:diff/staged"
    ],
    "rules": {
        // Если даже так правило будет доставлять больше неудобств, чем пользы, можно будет его отключить:
        // "playwright/no-conditional-in-test": "off"
    }
}